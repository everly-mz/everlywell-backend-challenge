<%= form_with(model: headline, local: true) do |form| %>
  <% if headline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(headline.errors.count, "error") %> prohibited this headline from being saved:</h2>

      <ul>
        <% headline.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :heading %>
    <%= form.text_field :heading %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
